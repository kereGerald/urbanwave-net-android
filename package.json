{
  "name": "urbanwave-net-android",
  "version": "1.0.0",
  "private": true,
  "description": "UrbanWave Net Android app — Capacitor shell loading the live production dashboard",
  "scripts": {
    "build:apk:debug": "cd android && ./gradlew assembleDebug",
    "sync": "npx cap sync android"
  },
  "devDependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/app": "^7.0.0",
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^7.0.0",
    "@capacitor/core": "^7.0.0",
    "@capacitor/splash-screen": "^7.0.0"
  }
}
